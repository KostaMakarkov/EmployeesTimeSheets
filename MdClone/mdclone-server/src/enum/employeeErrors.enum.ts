export enum EmployeeErrorsEnum {
  Duplicate = "Employee already exists",
  CreateError = "Error with creating employee",
  InvalidParameters = "Invalid parameters",
  DbError = "Error with DB",
}
